{
  "name": "setup-ocx",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action to install the OCX package manager",
  "scripts": {
    "build": "ncc build src/setup.ts -o dist/setup --license licenses.txt",
    "test": "bun test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/http-client": "^2.2.3",
    "@actions/tool-cache": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0"
  }
}
